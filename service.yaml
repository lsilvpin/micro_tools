apiVersion: v1
kind: Service
metadata:
  name: micro-tools-service
spec:
  selector:
    app: micro-tools
  ports:
    - protocol: TCP
      port: 32000
      targetPort: 8000
      nodePort: 8000
  type: NodePort
