apiVersion: v1
kind: Service
metadata:
  name: micro-tools-service
spec:
  selector:
    app: micro-tools
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 8000
      nodePort: 9000
  type: NodePort
